import os

# folder = pathlib.Path(ROOT_DIR + "/data")
# files = [f for f in folder.iterdir() if f.is_file()]
# filenames = [f.name for f in files]
# soup = BeautifulSoup(, "lxml")

# connection = sqlite3.connect('database/db.db')
# cursor = connection.cursor()
# connection.close()
